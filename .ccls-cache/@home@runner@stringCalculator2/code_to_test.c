#include <stdio.h>
/*
#include <string.h>

int main() {
    char input[] = "hello] world";
    char delimiters[] = ",.";

    char *result = strpbrk(input, delimiters);
    if (result) {
        printf("The input string contains the delimiter '%c'\n", *result);
    } else {
        printf("The input string does not contain any of the specified delimiters\n");
    }
    return 0;
}
*/